package Plants;

public class AppleTree extends Plants{

    public AppleTree(double preparationCost, double protectionCost, double yield, int growthLength, int info, double harvestCost, double price) {
        super(preparationCost, protectionCost, yield, growthLength, info, harvestCost, price);
    }
}
