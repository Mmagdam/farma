package Animals;
import Plants.Plants;
public class Chicken extends Animals{


    public Chicken(double price, double weightRate, double weight, int growingUpTime, double foodAmount, Plants foodType, boolean reproduce) {
        super(price, weightRate, weight, growingUpTime, foodAmount, foodType, reproduce);
    }
}
