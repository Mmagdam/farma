package Animals;

public class Pig extends Animals{

    public Pig(double price, double weightRate, int growingUpTime, double foodAmount, String foodType, boolean reproduce) {
        super(price, weightRate, growingUpTime, foodAmount, foodType, reproduce);
    }
}
